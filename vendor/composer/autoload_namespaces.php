<?php

// autoload_namespaces.php @generated by Composer
// Use/function: stores old PSR-0 namespace mappings.
// This project does not use PSR-0, so the returned array is empty.

// vendorDir points to the vendor folder.
$vendorDir = dirname(__DIR__);

// baseDir points to the project root folder.
$baseDir = dirname($vendorDir);

return array(
    // No PSR-0 namespaces are registered for this project.
);
